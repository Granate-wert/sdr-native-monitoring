[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "esw-dfl-sgram-native"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "esw_dfl._sgram_native"
python-source = "../../"
